---
phase: 04-build-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Makefile
  - .goreleaser.yml
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - BUILD-01
  - BUILD-02
  - BUILD-03

must_haves:
  truths:
    - "`make build` produces a binary named `mxlrcgo-svc` from `cmd/mxlrcgo-svc/`"
    - "GoReleaser config builds from `cmd/mxlrcgo-svc/` and names the binary `mxlrcgo-svc`"
    - "CI matrix build step uses `./cmd/mxlrcgo-svc` as the build path and outputs `mxlrcgo-svc`"
  artifacts:
    - path: "Makefile"
      provides: "Updated BINARY and build target"
      contains: "BINARY=mxlrcgo-svc"
    - path: ".goreleaser.yml"
      provides: "Updated GoReleaser build config"
      contains: "cmd/mxlrcgo-svc"
    - path: ".github/workflows/ci.yml"
      provides: "Updated CI matrix build step"
      contains: "mxlrcgo-svc"
  key_links:
    - from: "Makefile"
      to: "cmd/mxlrcgo-svc/"
      via: "go build -o $(BINARY) ./cmd/mxlrcgo-svc"
      pattern: "go build.*cmd/mxlrcgo-svc"
    - from: ".goreleaser.yml"
      to: "cmd/mxlrcgo-svc/"
      via: "main: ./cmd/mxlrcgo-svc"
      pattern: "main:.*cmd/mxlrcgo-svc"
---

<objective>
Update all build tooling to reference the new binary name (`mxlrcgo-svc`) and build path (`./cmd/mxlrcgo-svc/`).

Purpose: The restructuring moved the entry point from the root package to `cmd/mxlrcgo-svc/`. Build tools still point to the old location and binary name — this plan corrects all three: Makefile, GoReleaser, and CI.
Output: Three updated config files that correctly build `mxlrcgo-svc` from `./cmd/mxlrcgo-svc/`.
</objective>

<execution_context>
@/Users/jesse/Developer/mxlrc-go/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/jesse/Developer/mxlrc-go/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<!-- Current state that needs updating -->

Makefile (current — WRONG):
```makefile
BINARY=mxlrc-go
build:
	go build -o $(BINARY) .
```

.goreleaser.yml (current — WRONG):
```yaml
builds:
  - id: mxlrc-go
    main: .
    binary: mxlrc-go
```

.github/workflows/ci.yml build-matrix step (current — WRONG):
```bash
run: go build -ldflags="-s -w" -o mxlrc-go .
```

Target state:
- Binary name everywhere: `mxlrcgo-svc`
- Build path everywhere: `./cmd/mxlrcgo-svc`
- GoReleaser release.github.name: `mxlrcgo-svc` (GitHub repo renamed to match)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Makefile for new binary name and build path</name>
  <files>Makefile</files>
  <action>
Edit Makefile with the following changes:

1. `BINARY=mxlrc-go` → `BINARY=mxlrcgo-svc`
2. `go build -o $(BINARY) .` → `go build -o $(BINARY) ./cmd/mxlrcgo-svc`

No other changes needed. The `clean` target already uses `$(BINARY)` so it will clean the right file. The `run` target already uses `./$(BINARY)` so it stays correct.

Per BUILD-01.
  </action>
  <verify>
    <automated>grep -E "BINARY=mxlrcgo-svc" Makefile && grep -E "cmd/mxlrcgo-svc" Makefile && make build && test -f mxlrcgo-svc && echo "PASS"</automated>
  </verify>
  <done>`make build` succeeds and produces `./mxlrcgo-svc` binary from `cmd/mxlrcgo-svc/`</done>
</task>

<task type="auto">
  <name>Task 2: Update GoReleaser config for new binary name and build path</name>
  <files>.goreleaser.yml</files>
  <action>
Edit `.goreleaser.yml` with the following changes:

1. `id: mxlrc-go` → `id: mxlrcgo-svc`
2. `main: .` → `main: ./cmd/mxlrcgo-svc`
3. `binary: mxlrc-go` → `binary: mxlrcgo-svc`
4. `release.github.name: mxlrc-go` → `release.github.name: mxlrcgo-svc`

Per BUILD-02. The `ldflags: -s -w` and all platform matrix settings remain unchanged.
  </action>
  <verify>
    <automated>grep "id: mxlrcgo-svc" .goreleaser.yml && grep "main: ./cmd/mxlrcgo-svc" .goreleaser.yml && grep "binary: mxlrcgo-svc" .goreleaser.yml && echo "PASS"</automated>
  </verify>
  <done>GoReleaser config references `mxlrcgo-svc` as binary name and `./cmd/mxlrcgo-svc` as main path</done>
</task>

<task type="auto">
  <name>Task 3: Update CI workflow build step for new path and binary name</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Edit `.github/workflows/ci.yml`, find the `build-matrix` job's `Build binary` step:

Current:
```
run: go build -ldflags="-s -w" -o mxlrc-go .
```

Replace with:
```
run: go build -ldflags="-s -w" -o mxlrcgo-svc ./cmd/mxlrcgo-svc
```

Only this one line changes. All other CI workflow steps remain unchanged.

Per BUILD-03.
  </action>
  <verify>
    <automated>grep "mxlrcgo-svc ./cmd/mxlrcgo-svc" .github/workflows/ci.yml && echo "PASS"</automated>
  </verify>
  <done>CI build-matrix step uses correct binary name and build path</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config files → build tools | Build tool reads config; malformed config could produce incorrect binary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | Makefile / .goreleaser.yml | accept | Config files in version control; no secrets involved; worst outcome is failed build, not security issue |
| T-04-01-02 | Information Disclosure | .github/workflows/ci.yml | accept | CI builds public repo; no secrets in these config changes |
</threat_model>

<verification>
After all three tasks:

```bash
# Verify Makefile
make build && test -f mxlrcgo-svc && echo "Binary exists"
make clean && test ! -f mxlrcgo-svc && echo "Clean works"

# Verify GoReleaser config is valid YAML with correct fields
grep -c "mxlrcgo-svc" .goreleaser.yml  # Should be >= 3 (id, binary, release name)

# Verify CI config
grep "mxlrcgo-svc ./cmd/mxlrcgo-svc" .github/workflows/ci.yml

# Full build check
go build ./... && echo "Module builds clean"
```
</verification>

<success_criteria>
- `make build` produces `./mxlrcgo-svc` (not `mxlrc-go`)
- `make clean` removes `./mxlrcgo-svc`
- `.goreleaser.yml` has `id: mxlrcgo-svc`, `main: ./cmd/mxlrcgo-svc`, `binary: mxlrcgo-svc`
- CI `build-matrix` step builds from `./cmd/mxlrcgo-svc` with output `mxlrcgo-svc`
- `go build ./...` still passes (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/04-build-verification/04-01-SUMMARY.md`
</output>
