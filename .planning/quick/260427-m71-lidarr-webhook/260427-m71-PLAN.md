---
phase: quick
plan: 260427-m71
type: execute
wave: 1
depends_on:
  - 260427-m6
files_modified:
  - internal/server/
  - internal/config/
  - cmd/mxlrcgo-svc/
  - config.example.toml
autonomous: true
requirements:
  - issue-22
must_haves:
  truths:
    - "POST /api/v1/webhooks/lidarr accepts API keys from apikey query parameter or Bearer header"
    - "Download and TrackRetag enqueue work synchronously before returning 200"
    - "Grab and Rename are logged without enqueueing"
    - "Delete is accepted as cleanup/no-op for now"
    - "Access logs redact apikey query values"
    - "Focused handler tests and go test ./... pass"
---

<objective>
Implement issue #22: HTTP server and Lidarr webhook handler.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: HTTP handler package</name>
  <files>internal/server/</files>
  <done>
    - Lidarr webhook JSON is decoded with a bounded body
    - API key auth uses the existing auth.Service scopes
    - Event routing matches issue #22
  </done>
</task>

<task type="auto">
  <name>Task 2: Configuration and CLI wiring</name>
  <files>internal/config/config.go, cmd/mxlrcgo-svc/main.go, config.example.toml</files>
  <done>
    - Server listen address is configurable
    - CLI can run the HTTP server without requiring positional song input
  </done>
</task>

<task type="auto">
  <name>Task 3: Verification</name>
  <files>all touched files</files>
  <done>
    - gofmt applied
    - go test ./... passes
  </done>
</task>

</tasks>

<verification>
```bash
go test ./...
```
</verification>
