# Summary: 260427-m71 Lidarr Webhook

Implemented issue #22.

- Added `internal/server` with `POST /api/v1/webhooks/lidarr`.
- Added API key auth from `apikey` query parameter or `Authorization: Bearer ...`.
- Enqueued `Download` and `TrackRetag` events synchronously before returning `200`.
- Logged `Grab` and `Rename` events without enqueueing.
- Cleaned pending/failed queue rows for `Delete` events when track metadata is present.
- Redacted `apikey` values in HTTP access logs.
- Added `--serve`, `--listen`, `[server]` config, and `MXLRC_SERVER_ADDR` / `MXLRC_WEBHOOK_API_KEY` env overrides.

Verification:

```bash
go test ./...
```
