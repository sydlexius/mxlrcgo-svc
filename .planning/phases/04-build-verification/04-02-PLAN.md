---
phase: 04-build-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
autonomous: true
requirements:
  - BUILD-06
  - BUILD-08

must_haves:
  truths:
    - "go.mod declares `go 1.24` as minimum version"
    - "go-arg is at v1.6.1"
    - "fastjson is at v1.6.10"
    - "golang.org/x/text is at its latest version"
    - "dhowden/tag is at its latest version"
    - "`go build ./...` and `go test ./...` pass after upgrades"
  artifacts:
    - path: "go.mod"
      provides: "Updated module file with bumped Go version and dependencies"
      contains: "go 1.24"
    - path: "go.sum"
      provides: "Updated checksums for new dependency versions"
  key_links:
    - from: "go.mod"
      to: "go.sum"
      via: "go mod tidy"
      pattern: "go mod tidy"
---

<objective>
Bump the minimum Go version to 1.24 and upgrade four direct dependencies to their current stable versions.

Purpose: The restructured codebase is ready for upgraded tooling. Staying on outdated dependencies creates security exposure and misses API improvements. Go 1.22 minimum is conservatively low — 1.24 reflects what the code actually uses (slices stdlib, log/slog).
Output: Updated `go.mod` and `go.sum` with Go 1.24 and upgraded direct dependencies, all tests still passing.
</objective>

<execution_context>
@/Users/jesse/Developer/mxlrc-go/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/jesse/Developer/mxlrc-go/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<!-- Current go.mod state -->
Current go.mod:
```
module github.com/sydlexius/mxlrcgo-svc

go 1.22

require (
    github.com/alexflint/go-arg v1.4.3
    github.com/dhowden/tag v0.0.0-20220618230019-adf36e896086
    github.com/joho/godotenv v1.5.1
    github.com/valyala/fastjson v1.6.3
    golang.org/x/text v0.3.8
)

require github.com/alexflint/go-scalar v1.1.0 // indirect
```

Target versions (from `go list -m -u all`):
- go-arg: v1.4.3 → v1.6.1
- fastjson: v1.6.3 → v1.6.10
- x/text: v0.3.8 → latest (v0.36.0 at time of writing)
- dhowden/tag: v0.0.0-20220618230019 → latest (v0.0.0-20240417053706)
- godotenv: v1.5.1 — already latest, no change needed
- Go version: 1.22 → 1.24
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump Go minimum version to 1.24</name>
  <files>go.mod</files>
  <action>
Edit `go.mod` and change:
```
go 1.22
```
to:
```
go 1.24
```

Then run `go mod tidy` to update checksums and verify the build still compiles. The codebase uses `slices.Contains` (stable since Go 1.21) and `log/slog` (stable since Go 1.21), so 1.24 is valid and conservative.

Per BUILD-08.
  </action>
  <verify>
    <automated>grep "^go 1.24" go.mod && go build ./... && echo "PASS"</automated>
  </verify>
  <done>`go.mod` shows `go 1.24` and `go build ./...` succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade direct dependencies to target versions</name>
  <files>go.mod, go.sum</files>
  <action>
Run the following `go get` commands in order to upgrade each dependency to the target version:

```bash
go get github.com/alexflint/go-arg@v1.6.1
go get github.com/valyala/fastjson@v1.6.10
go get golang.org/x/text@latest
go get github.com/dhowden/tag@latest
go mod tidy
```

After each upgrade, `go build ./...` must still compile. If any upgrade introduces an API-breaking change (unlikely for these libraries — they follow semver), investigate before proceeding.

Notes on what changed in each dependency:
- **go-arg v1.6.1**: Minor additions to arg parsing API; existing struct tags remain compatible
- **fastjson v1.6.10**: Bug fixes only; no API changes
- **x/text**: NFKC normalization API used in `internal/scanner/slugify.go` is stable
- **dhowden/tag**: Audio tag reading; only the `ReadFrom` function is used, which is stable

Per BUILD-06.
  </action>
  <verify>
    <automated>grep "go-arg v1.6.1" go.mod && grep "fastjson v1.6.10" go.mod && go build ./... && go test ./... && echo "PASS"</automated>
  </verify>
  <done>All four dependencies upgraded, `go mod tidy` clean, all tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| go get → module proxy | Dependency fetched from proxy.golang.org; checksums verified against sum.golang.org |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering | go.sum | mitigate | `go mod tidy` regenerates go.sum; checksums verified by Go toolchain against sum.golang.org |
| T-04-02-02 | Elevation of Privilege | dependency upgrade | accept | All upgrades are patch/minor semver; API-compatible; no new permissions or capabilities added |
</threat_model>

<verification>
```bash
# Verify Go version bump
grep "^go 1.24" go.mod

# Verify dependency versions
grep "go-arg v1.6.1" go.mod
grep "fastjson v1.6.10" go.mod
grep "golang.org/x/text" go.mod  # version should be > v0.3.8
grep "dhowden/tag" go.mod        # version should be > 20220618

# Full build and test
go build ./...
go test ./...
go vet ./...
```
</verification>

<success_criteria>
- `go.mod` shows `go 1.24`
- `go-arg` pinned at `v1.6.1`
- `fastjson` pinned at `v1.6.10`
- `x/text` updated beyond `v0.3.8`
- `dhowden/tag` updated beyond `20220618`
- `go build ./...` succeeds
- `go test ./...` all pass
- `go mod tidy` produces no changes (go.sum already clean)
</success_criteria>

<output>
After completion, create `.planning/phases/04-build-verification/04-02-SUMMARY.md`
</output>
